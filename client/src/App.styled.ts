import { styled } from '@stitches/react';

export const appWraper = styled('div', {
    display: 'flex',
    flexDirection: 'column',
    gap: '2rem'
})
export const combinationListWraper = styled('div', {
    display: 'flex',
    gap: '1rem'
})
